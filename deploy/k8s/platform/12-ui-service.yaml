apiVersion: v1
kind: Service
metadata:
  name: opamp-fleet-ui
  namespace: opamp-system
spec:
  selector:
    app.kubernetes.io/name: opamp-fleet-ui
  ports:
    - name: http
      port: 8081
      targetPort: http
