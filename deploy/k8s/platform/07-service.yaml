apiVersion: v1
kind: Service
metadata:
  name: opamp-server
  namespace: opamp-system
spec:
  selector:
    app.kubernetes.io/name: opamp-server
  ports:
    - name: opamp
      port: 4320
      targetPort: opamp
    - name: api
      port: 8080
      targetPort: api
