{{- if not .Values.auth.agentTokens.existingSecret -}}
{{- if not .Values.auth.agentTokens.tokens -}}
{{- fail "auth.agentTokens: set either existingSecret or a non-empty tokens list" -}}
{{- end -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "opamp-fleet-server.agentTokensSecretName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "opamp-fleet-server.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{ .Values.auth.agentTokens.existingSecretKey }}: |
    {{- range .Values.auth.agentTokens.tokens }}
    {{ . }}
    {{- end }}
{{- end -}}
